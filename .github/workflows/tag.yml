name: Tag

on:
  push:
    branches: [ CVAT* ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Run a one-line script
        run: |
          ls
          pwd
          echo $GITHUB_WORKSPACE
